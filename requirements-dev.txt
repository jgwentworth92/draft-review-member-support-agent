# Development/test dependencies. Installs runtime deps plus test tooling.
# (httpx moved to requirements.txt - it is now a direct runtime dependency.)
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<9.0
